# Unnamed_Chess

[드디어 일한 김찬서 20241005.xlsx](https://prod-files-secure.s3.us-west-2.amazonaws.com/c6d855d3-7afb-45b9-8fb7-bd4ec9c822a0/91be2f94-101c-42e7-8548-d3077889a747/%EB%93%9C%EB%94%94%EC%96%B4_%EC%9D%BC%ED%95%9C_%EA%B9%80%EC%B0%AC%EC%84%9C_20241005.xlsx)

> [!TIP]
> 🟩 **수정한 내용 — 위 증강표를 이렇게 고쳐서 게임에 넣었습니다.**
>
> | 파일 | 내용 |
> |---|---|
> | 📊 [**무제체스_증강표_v2.xlsx**](docs/무제체스_증강표_v2.xlsx) | 게임에 실제 적용된 증강 **72종**. 원안과 같은 (기물 × 처치수) 격자, 칸마다 정확히 3개, 용어 해설, 전역 룰, 밸런스 메모, `개정내역` 시트 |
> | 🔧 [**무제체스_증강_구현표.xlsx**](docs/무제체스_증강_구현표.xlsx) | 72종을 **어떻게 구현했는지** — 발동 시점(훅), 대상 지정 여부, 잠금 조건, 용어. `augments.js` 를 직접 파싱해 만들어서 코드와 어긋날 수 없습니다 |
> | 📝 [기획검토.md](docs/기획검토.md) | 개정 35건의 사유 전문 |
> | 📋 [수정한 내용](docs/수정한_내용.md) | 날짜별 변경 기록. 큰 변화·새 기능은 새 브랜치로, 나머지는 여기에 |
> | 💬 [피드백.md](docs/피드백.md) | 받은 피드백 원문 |
>
> **원안에서 고친 것 — 큰 것 다섯 가지**
>
> 1. **킹 보호 (전역 룰 신설)** — 킹은 제거 · 포영 · 변이 · 교환 · 지정불가의 대상이 되지 않습니다.
>    원안대로면 첫 처치로 얻은 룩 증강 하나(`R1b` 줄 전멸)로 상대 킹이 사라져 게임이 끝납니다.
> 2. **체크 회피 우선 (전역 룰 신설)** — 이동을 제한하는 증강은 체크를 벗어나는 수를 막지 못합니다.
>    안 그러면 둘 수 있는 수가 0개가 되어 게임이 멈춥니다.
> 3. **킹 증강 재설계** — '킹 = 플레이어' 라는 의도에 맞춰, 킹을 직접 강화하던 2개(두 칸 이동 · 퀸처럼 이동)를 게임 진행에 개입하는 메타 증강으로 교체했습니다.
> 4. **킹 11개 티어** — '킹으로 3킬 시 승리' 는 달성이 사실상 불가능해서 **상대 킹 포위**로 바꿨습니다.
> 5. **증강을 얻는 방식 확정** — 원안에는 격자만 있고 획득 절차가 없었습니다.
>    칸마다 선택지가 정확히 3개라는 점을 그대로 살려, **처치를 해낸 기물의 칸 하나를 통째로 펼치고 그중 1개**를 고릅니다.
>
> 나머지 30건은 대부분 **두 가지로 읽히는 문구를 하나로 확정한 것**입니다.
> (예: `Q1a` "대각선에 해당하는 칸" → 긴 대각선 2개 / `B1a` `"4~5열"` → `4~5랭크`)

---

### 이 문서를 읽는 법

> [!TIP]
> 🟩 **수정한 내용** — 원안 README 에는 없던 절입니다.

이 README는 **원래 기획 문서를 한 글자도 지우지 않고 그대로 두고**, 만들면서 달라진 부분만 바로 아래에 덧붙인 것입니다.

| 보이는 것 | 뜻 |
|---|---|
| 아무 표시 없는 본문 | **원안 그대로.** 한 글자도 손대지 않았습니다 |
| 🟩 **수정한 내용** (초록 줄 블록) | **우리가 고치거나 새로 넣은 내용** |

즉 **왼쪽에 초록 줄이 그어진 블록만 따라 읽으면 원안에서 무엇이 달라졌는지 전부 알 수 있습니다.**

초록 블록은 두 가지 자리에 있습니다.

- **원안 문단 바로 아래의 인용 블록** — 그 위 원안 내용을 무엇으로 어떻게 바꿨는지
- **절 제목 바로 아래** — 그 절 전체가 원안에 없던 새 내용이라는 표시

원안 전문만 따로 보려면 [`docs/기획원문.md`](docs/기획원문.md) 를 보세요.

---

## 실행하기

> [!TIP]
> 🟩 **수정한 내용** — 원안 README 에는 없던 절입니다.

**웹 브라우저만 있으면 됩니다.** 설치도 빌드도 필요 없습니다. 파이썬은 선택 사항입니다.

| 환경 | 방법 |
|---|---|
| Windows | **`무제체스 실행.bat`** 더블클릭 |
| macOS · Linux | 터미널에서 `python3 tools/launch.py` |
| 아무 데서나 | `game/index.html` 을 브라우저로 직접 열기 |

`무제체스 실행.bat` 은 파이썬이 있으면 로컬 서버를 띄우고 브라우저를 자동으로 열며, 없으면 `game/index.html` 을 그대로 엽니다. 둘 다 정상 동작합니다.

```bash
python tools/launch.py
```

- 포트는 8777부터 비어있는 것을 알아서 골라 씁니다.
- **이미 떠 있으면 새로 띄우지 않고 그 창의 주소를 엽니다.** 서버가 둘이 되면 온라인 대전의 방이 갈리기 때문입니다.
- 끄려면 창을 닫거나 `Ctrl+C`.
- 캐시가 꺼져 있어 파일을 고치고 새로고침하면 곧바로 반영됩니다.

기물은 **클릭하거나 끌어다 놓아** 둡니다. 메인 화면에서 AI 대전(초급 · 중급 · 고급) / 2인 대전 / 온라인 대전과 제한시간을 고르고 시작합니다.

**온라인 대전도 따로 준비할 게 없습니다.** `tools/launch.py` 가 중계 서버(`server/relay.py`)를 함께 띄웁니다. 파이썬 표준 라이브러리만 쓰므로 설치할 패키지가 없습니다. 실행할 때 콘솔에 어느 모드인지 찍힙니다.

> 🟩 **수정한 내용 — 중계 서버를 Node 에서 파이썬으로 옮겼습니다.**
>
> 처음에는 Node + `ws` 패키지로 만들었는데, `node_modules` 는 저장소에 올리지 않으므로
> **다른 컴퓨터에서 받으면 온라인 대전이 조용히 꺼져 있었습니다.** 실행기가 정적 서버로 떨어지고
> "이 주소에서는 온라인 대전을 쓸 수 없습니다" 만 떴습니다.
>
> 이 게임의 장점은 원래 "설치도 빌드도 필요 없다" 인데 온라인을 붙이면서 그걸 깨뜨린 셈이라,
> WebSocket 중계를 파이썬 표준 라이브러리로 다시 썼습니다. 파이썬은 실행기가 이미 쓰고 있어서
> 새로 붙는 의존성이 없습니다. 프로토콜은 그대로라 클라이언트는 한 줄도 바뀌지 않았습니다.

---

## 개요

일반적인 체스 룰 + 특정 조건에서 기물을 선택해서 강화하는 방식

더욱 다양한 경우의 수와 유연한 플레이 가능

기물 간의 적절한 밸런스 조절 필수

> [!TIP]
> 🟩 **수정한 내용 — 원안대로 구현했습니다.**
>
> 강화(이하 *증강*)는 **72종 전부** 실제로 동작합니다.
> 외부 라이브러리나 이미지 없이 순수 HTML/CSS/JS 로만 되어 있습니다.

## 게임 구성

### 1. 플레이어

체스와 동일하게 각 플레이어는 자신의 턴마다 통상적으로 하나의 기물을 움직일 수 있다.

### 2. 게임진행

기본적으로 턴제 게임 형식으로 진행된다. 서로 한 번씩 번갈아가며 플레이 하고 총 16개의 기물 중 하나를 골라 그 기물이 갈 수 있는 루트로 기물을 움직인다. (한 번의 플레이가 한 턴이다. 즉, 서로 한 번씩 두면 2턴이 지난 것)

플레이어가 일정 개수의 적을 죽였을 때, 어떤 기물로 적을 죽였냐에 따라 그 기물에만 추가효과를 주는 부스트를 선택할 수 있음.

ex) 폰으로 적을 죽인다면, 다음의 조건 3개 중 하나를 선택해 폰을 강화할 수 있음

1. 다음 상대턴동안, 지정한 자신의 폰 하나가 지정불가 상태가 됩니다. 
2. 다음 1회, 폰이 두 번 전진할 수 있습니다. (전진할 곳에 다른 기물이 있을 시 움직일 수 없습니다)
3. 자신의 폰이 좌우로도 움직일 수 있음 

> [!TIP]
> 🟩 **수정한 내용 — 원안 그대로 "무엇으로 잡았는가" 기준입니다.**
>
> 증강표가 칸마다 선택지를 정확히 3개씩 두고 있어서, 그 칸 하나를 그대로 펼치는 구조로 만들었습니다.
> 어느 칸을 펼칠지는 원안의 *"어떤 기물로 적을 죽였냐에 따라"* 를 따라 **처치한 쪽 기물**로 정합니다 —
> **퀸으로 폰을 잡으면 퀸 증강 3개**가 뜹니다. 잡힌 기물이 무엇인지는 상관없습니다.
>
> - 그중 **하나만** 고릅니다.
> - 폰이 승격하면서 잡은 경우에는 **승격 전 종류(폰)** 를 씁니다. 잡는 순간 두었던 기물이 폰이기 때문입니다.
> - **지금 발동해도 아무 일이 없는 증강은 고를 수 없습니다.** 빨간 ✕ 와 이유가 표시되고 클릭이 막힙니다. (예: 아군 룩이 하나도 없는데 룩 증강)
> - 고른 칸이 통째로 잠겨 있으면(예: 내 퀸이 이미 죽었는데 퀸으로 잡을 수는 없으니 이런 일은 드물지만, 조건이 까다로운 칸에서는 생깁니다) 고를 수 있는 다른 기물 칸으로 넘어갑니다.
>
> **위 폰 예시 3개는 실제 표와 다릅니다.** 1·2번은 1개 티어가 맞지만, 3번 *"좌우로 움직임"* 은 표에서 **6개 티어**(`P6a`)입니다. 1개 티어의 세 번째 선택지는 `P1c`(적을 처치한 폰이 가장 가까운 상대 폰과 함께 포영)입니다. 게임은 **표를 따랐습니다.**

### 3. 게임 목표

일반적인 체스의 승리 요건과 똑같이, 상대의 킹을 체크메이트로 만들거나 제거하면 승리.

체크 상태일경우, 메세지와 음성으로 플레이어에게 알림.

일반적인 체스에서 진행되는 오프닝 - 미드 자리먹기 - 비숍/나이트 교환 - 퀸 교환 - 후반 룩폰싸움으로 이어지는 단조로운 행동패턴 타파.  

부스트에 따른 다양한 선택지로 게임마다 색다른 패턴 추구. 

> [!TIP]
> 🟩 **수정한 내용 — "킹을 제거하면 승리" 는 뺐습니다. 그대로 두면 게임이 무너집니다.**
>
> 증강표에는 판을 쓸어버리는 효과가 여럿 있습니다(`R1b` 줄 전멸, `N1c` 주변 전멸, `Q11a`, `B6c` 등). 킹 예외가 없으면 **첫 처치로 얻은 룩 1개 티어 증강 하나만으로 상대 킹이 사라져 게임이 끝납니다.** 체크메이트라는 승리 조건 자체가 의미를 잃습니다.
>
> 그래서 전역 룰을 넣었습니다: **킹은 제거 · 포영 · 변이 · 교환 · 지정불가의 대상이 되지 않습니다.** 킹을 잃는 유일한 방법은 체크메이트입니다.
>
> 대신 "킹을 몰아붙여 끝낸다" 는 감각은 킹 11개 티어 증강(`K11b`)으로 살렸습니다 — **아군 기물이 상대 킹의 상하좌우 4칸 중 3칸을 점거하면 즉시 승리.**
>
> *체크 시 메시지와 음성 알림* 은 원안대로 구현했습니다(빨간 강조 + 경고음).

## 게임 요소

기물 별 구체적인 강화요소는 다음과 같으며 각 단계에 따라 강화선택지가 달라짐. (처치 수는 누적이 아닌 각 단계마다 집계. 즉, 총 기물을 1개, 3개, 6개, 11개 처치 시 활성화)

1단계: 적 기물 1개 처치

2단계: 적 기물 2개 처치

3단계: 적 기물 3개 처치

4단계: 적 기물 5개 처치

영구지속/턴제한/횟수제한으로 능력은 구분됨 (가능한 하나만 포함)

부스트에 '이어서 움직인다'가 있으면 안됨. (상대 입장에서는 2수를 봐야하므로 지나치게 생각해야 할 수가 많아짐) 

> [!TIP]
> 🟩 **수정한 내용 — 단계 수치가 원안 안에서 서로 어긋납니다.**
>
> 괄호 안은 **1 · 3 · 6 · 11 개 처치**라고 하는데, 바로 아래 목록은 **1 · 2 · 3 · 5 개**로 되어 있습니다. 증강표 자체가 1 · 3 · 6 · 11 로 되어 있어서 **게임은 1 · 3 · 6 · 11 을 따랐습니다.**
>
> 또 *"누적이 아닌 각 단계마다 집계"* 라고 되어 있지만 1·3·6·11 이라는 값 자체가 누적 처치 수로 읽히므로 **누적 처치 수**로 구현했습니다.
>
> **'이어서 움직인다' 금지 규칙을 어기는 증강이 표에 있습니다.** 킹 6개 티어 `K6a` *"다른 기물과 함께 킹을 한 번 움직일 수 있습니다"* 가 정확히 그 경우입니다. 표에 있던 것이라 일단 그대로 구현했지만, 원안의 금지 규칙을 지키시려면 빼야 합니다.

**처치 별 벨런스**

```
1개 밸런스: 킹 > 룩 > 퀸 > 비숍 = 나이트 = 폰
3개 벨런스: 룩 > 킹 >> 퀸 = 나이트 = 비숍 > 폰
6개 벨런스: 킹 > 폰 > 룩 = 나이트 = 비숍  = 퀸
11개 벨런스: 폰 > 나이트, 비숍 > 퀸 > 킹 > 룩
```

**기물 별 벨런스**

```
폰: 11개 > 6개 >> 1개 = 3개
나이트: 11개 >> 6개 > 3개 = 1개
비숍: 11개 >> 1개 > 3개 = 6개
룩: 1개 > 3개 >> 6개 > 11개
퀸: 11개 >>> 1개 = 6개 > 3개
킹: 6개 = 1개 > 3개 > 11개
```


> [!TIP]
> 🟩 **수정한 내용 — 자동 대전 40판을 돌려본 결과입니다.**
>
> | 항목 | 값 |
> |---|---|
> | 한 판 평균 총 처치 수 | 10.7 (양측 합산) |
> | 한 진영당 평균 획득 증강 | 2.0개 |
> | 도달 티어 분포 (80진영) | 0단계 13 / 1단계 10 / 2단계 17 / **3단계 32** / **4단계 8** |
>
> **11개 티어에 도달하는 진영이 10% 뿐입니다.** 가장 공들인 증강 18개가 거의 쓰이지 않습니다. 위의 "11개 벨런스" 평가가 실전보다 추정에 가까울 수밖에 없는 이유이기도 합니다.
>
> 한때 대응으로 킹 1개 티어에 증강 하나를 더 넣어 봤지만 되돌렸습니다. **칸마다 선택지가 정확히 3개**라는 원안의 규칙이 드래프트 구조 자체(칸 하나를 그대로 펼침)를 떠받치고 있어서, 한 칸만 4개가 되면 그 칸에서만 규칙이 달라집니다. 지금은 24칸 전부 3개입니다.
>
> 그래서 이 문제는 **아직 열려 있습니다.** 검토만 하고 적용하지 않은 안: **티어를 1 · 3 · 6 · 9 로 낮추기.** 평균 처치 수가 10.7이라 11은 사실상 한쪽이 판을 압도했을 때만 열립니다. 의도하신 희소성일 수도 있어 임의로 바꾸지 않았습니다.
>
> 밸런스 때문에 조정한 증강도 있습니다. `R1b`(룩 1개, 줄 전멸)는 첫 처치에 얻는 것치고 과해서 **사거리 4칸 + 아군 포함 + 킹 제외**로 낮췄습니다. 제거는 처치 카운트에 안 들어가므로 자기 진행도 늦어지는 자연스러운 대가가 생깁니다.

## 게임 컨텐츠

### 1. 게임 모드

- 일반 모드
- 랭크 모드
- 겜블러 모드 (모든 부스트의 순서가 랜덤)

> [!TIP]
> 🟩 **수정한 내용 — 일반 모드만 있습니다.**
>
> 구현된 것: **AI 대전**(초급 · 중급 · 고급), **2인 대전**(한 화면에서 번갈아), **온라인 대전**(방 코드로 각자 집에서).
> 랭크 모드는 계정이 필요해 손대지 않았습니다. 겜블러 모드는 아직 없습니다.

### 2. 기타 컨텐츠

- 업적

업적은 크게 도전형 업적과 누적형 업적으로 분류. 도전형의 경우 여러 게임 중 특별한 경우에만 나오는 경우(ex. 서로의 킹만 남는 경우)와 같이 1회성으로 일어나는 이벤트에 대해 업적을 부여한다. 누적형의 경우 "폰 100회 처치하기"와 같이 게임을 하며 자연스럽게 달성할 수 있는 업적을 의미한다. 

업적에 대한 보상은 스킨, 이모티콘, 칭호 등으로 한다. 

> [!TIP]
> 🟩 **수정한 내용 — 업적은 없습니다. 대신 전적 기록을 넣었습니다.**
>
> 메인 화면에서 승 · 패 · 무, 승률, 난이도별 성적, 최근 8판을 볼 수 있습니다.
> 기본은 브라우저에 저장되고, **로그인하면 계정에 합쳐져 다른 기기에서도 이어집니다** (아래 '계정' 절).
> 업적을 붙인다면 이 기록이 그 토대가 됩니다.

- 환경설정

기본적인 음향(배경음, 효과음)조절과 게임 나가기, 항복 기능을 지원한다. 

> [!TIP]
> 🟩 **수정한 내용 — 음향 조절 · 게임 나가기 · 항복 모두 있습니다.**
>
> 볼륨 슬라이더(메인 · 인게임 양쪽, 설정은 저장됨)와 `← 메인` 버튼, 헤더의 **항복** 버튼이 있습니다.
> 항복은 AI · 2인 대전에서는 지금 둘 차례가, 온라인에서는 내가 집니다. 온라인은 상대에게도 전달되고 전적에 패배로 남습니다.
> 승패를 안 남기는 '중단' 은 두었다가 뺐습니다 — 온라인에서 질 것 같을 때 빠져나가는 데 쓰일 수 있어서입니다.

- 음향

배경음과 효과음이 존재. 배경음은 각각 메인메뉴, 게임매칭, 인게임 배경음이 존재하며 효과음은 터치, 승리, 패배, 기물 조작 효과음이 존재한다.

> [!TIP]
> 🟩 **수정한 내용 — 효과음만 있습니다. 배경음은 없습니다.**
>
> 착수 · 처치 · 캐슬링 · 승격 · 체크 · 증강 발동 · 초읽기 · 승리 · 패배 · 무승부에 각각 다른 소리가 납니다.
> **외부 음원 파일 없이 Web Audio 로 합성**해서, 저장소에 음원을 넣지 않고도 `file://` 에서 소리가 납니다.

- 스킨

스킨은 플레이트, 기물 스킨이 존재한다. 플레이트는 선공 플레이어의 플레이트를 따라가며 기물 스킨은 자신에게만 보인다. 

- 이모티콘

게임 내에서 사용할 수 있는 이모티콘. 각 턴에 최대 2번씩만 사용가능(도배로 인한 불쾌감 및 가시성을 위함)하며 기본 이모티콘, 유료 이모티콘, 업적 이모티콘으로 구성

- 칭호

게임 내 닉네임 위에 기입되는 칭호. 업적으로만 구할 수 있으며 리소스가 충분하다면, 움직이거나 색이 있는 칭호를 포함한다.

> [!TIP]
> 🟩 **수정한 내용 — 스킨 · 칭호는 없고, 이모티콘은 온라인 대화에만 있습니다.**
>
> 온라인 대전의 **대화 탭**에서 이모티콘 10개를 한 번 눌러 바로 보낼 수 있습니다. 대국 중에 한글을 타자하는 건 부담이라 빠른 반응은 이쪽으로 가게 했습니다.
> 기본 · 유료 · 업적 이모티콘 같은 구분과 턴당 횟수 제한은 없습니다.
> 스킨과 칭호는 계정 시스템이 전제라 아직 붙일 자리가 없습니다.

---

## 원안에 없었지만 만들면서 필요해진 것

> [!TIP]
> 🟩 **수정한 내용** — 원안 README 에는 없던 절입니다.

### 전역 룰 — 체크 회피 우선

이동을 제한하는 증강(`R6a`/`R6b` 룩 기준 위 · 아래 이동 금지, `N3b` 나이트만 이동)이 체크와 겹치면 **둘 수 있는 수가 0개**가 되어 게임이 멈춥니다. 체크메이트도 스테일메이트도 아닌 상태입니다.

→ **이동 제한은 "체크를 벗어나는 수" 에는 적용되지 않습니다.**

### 지정불가 기물의 처리

지정불가는 양쪽 다 움직일 수도, 처치할 수도 없습니다. 단 위 규칙에 따라 **체크에 걸렸을 때는 예외**로 움직일 수 있습니다. 상대의 지정불가 기물은 잡을 수 없으므로, 지속 시간이 끝날 때까지는 킹을 피신시키거나 체크를 걸어 푸는 수밖에 없습니다. 기물 위에 파란 배지로 **남은 수**가 표시됩니다.

### 무슨 일이 일어났는지 보여주기

증강은 내가 두지 않은 기물을 옮기고, 없애고, 다른 기물로 바꿉니다. 그게 왜 일어났는지 세 군데에서 알려줍니다.

- **화면 한가운데 카드** — 증강을 얻으면 크게 뜹니다. 비밀 증강은 발동 전까지 가려지고, 발동하는 순간 공개됩니다.
- **판 위 금색 반짝임** — 증강이 판을 바꾸면 바뀐 칸이 빛나고, 어느 증강 때문인지 `⚡ [ID]` 로 남습니다.
- **활성 효과 패널** — 지금 걸린 효과가 남은 수와 함께 나열됩니다.
- **도약 경로** — 나이트처럼 건너뛰는 수는 어느 길로 간 것인지가 판에 안 남습니다.
  기물을 고르고 갈 곳에 마우스를 올리면 지나갈 칸에 순번이 찍히고, 두고 나면 잠깐 같은 경로가 뜹니다.
  **상대 화면에도 똑같이** 뜨고, 진영 스트립의 마지막 수 버튼을 누르면 언제든 다시 볼 수 있습니다.
  `N11b`(범위 2배 · 직선 두 칸 + 대각선 두 칸)처럼 움직임이 낯선 증강일수록 이게 없으면 납득이 안 됩니다.

훅 실행 전후로 판을 통째로 비교해 바뀐 칸을 자동으로 찾아내는 방식이라, **72종 전부에 개별 코드 없이** 적용됩니다.

> 🟩 **수정한 내용 — 상대 화면에서도 보이게, 조건형 증강은 지금 값을 보이게.**
>
> - **상대 증강이 내 기물을 지우면** 온라인에서는 이쪽 화면에서 기물이 소리 없이 사라졌습니다. 받은 판을 받기 전과 비교해
>   '방금 둔 수' 가 아닌데 바뀐 칸을 빛내고, 상대 화면에만 떴던 `⚡ [ID]` 줄을 배너로 보여 줍니다.
> - **`B3a` · `B3b`(점수 합이 홀수/짝수면 폰 소환)** 카드에 지금 아군 점수 합과 홀짝, 조건 충족 여부를 적습니다. 판정은 한 번뿐이라
>   끝나면 '판정 끝 — 소환함 / 불발' 로 남고, 판정 순간에는 가운데 카드가 뜹니다. 불발이면 토스트 한 줄뿐이라 증강이 그냥 사라진 것처럼 보였습니다.
> - 상대가 얻은 증강은 **비밀이 아니면 '상대의 증강 획득'** 으로, 비밀이면 발동할 때 '공개' 로 알립니다. 예전에는 비밀도 아닌 첫 증강이
>   "비밀 증강이 공개되었습니다" 로 떴습니다 — `revealed` 에 비밀이 아닌 증강도 얻는 순간 들어가는데, 받는 쪽이 전부 '공개' 로 읽어서였습니다.

### 온라인 대전 — 방 코드로 각자 집에서

메인 화면에서 **방 만들기** 를 누르면 5자리 코드가 나옵니다. 상대가 그 코드로 참가하면 바로 시작합니다.
각자 자기 색이 아래로 오게 판이 놓이고, 새로고침해도 두던 자리로 돌아옵니다.

**서버는 체스를 모릅니다.** 방을 열어 두 사람을 이어 주고, 한쪽이 보낸 판을 반대쪽에 넘길 뿐입니다.
`server/relay.py` 한 파일이고, 파이썬 표준 라이브러리만 씁니다(WebSocket 핸드셰이크·프레이밍 포함).

판정을 서버가 하지 않는 이유는 **비밀 증강** 때문입니다.
입력만 주고받는 방식(lockstep)으로 만들면 양쪽이 같은 계산을 돌려야 하고, 그러려면 상대 컴퓨터도 내 비밀 증강이
무엇인지 알아야 합니다. 그래서 **판을 바꾼 쪽이 결과를 보내고, 받는 쪽은 계산하지 않고 그대로 채택**합니다.
덤으로 양쪽 판이 어긋날 수가 없고, 재접속도 마지막 판만 다시 주면 끝납니다.

경계는 코드에 이미 있었습니다 — `doMove()` 의 마지막 줄 `await beginTurn(G.turn)` 입니다.
그 앞은 둔 사람이, 그 뒤(`onSched` 로 부활 대상을 고르는 `B3c` 같은 것)는 차례가 시작되는 쪽이 답해야 합니다.

**비밀 증강을 가리는 방법**

내 비밀 증강 id 는 `?b1` 같은 가면으로 바꿔 보냅니다. 가면에는 **기물과 티어만** 담습니다 —
지금 화면이 숨김 증강에 보여 주는 것과 정확히 같은 양입니다. 진짜 id 는 발동해서 공개되는 순간부터 실립니다.

id 는 목록 말고도 세 군데에 남아 있었고 전부 막았습니다.

| 새던 곳 | 처리 |
|---|---|
| 진행 기록 (`흑 증강 획득: [B1c] …`) | 비밀이면 `비밀 (비숍 1개) — 발동 전까지 비공개` 로만 적습니다 |
| 예약 효과의 `tag` (`sched tag:'B1c'`) | 가면으로 바꿉니다. 상대는 내 예약을 발동시키지 않으므로 문제없습니다 |
| `flags` 의 키 (`flags.b.B6c`) | 들어냅니다. 엔진이 읽는 flags 는 `P1b` · `R3a` · `Q1aReady` 뿐이라 상대 계산이 틀어지지 않습니다 |

> 🟩 **수정한 내용 — 기록 누출은 AI 대전에도 있던 결함이었습니다.**
>
> 증강 획득 기록이 id 와 문구를 그대로 적고 있어서, **AI가 고른 비밀 증강도 진행 기록만 읽으면 다 보였습니다.**
> 온라인을 만들다 발견해 같이 고쳤습니다.

> 🟩 **수정한 내용 — 온라인에서만 어긋나던 것들.**
>
> | 증상 | 원인 · 처리 |
> |---|---|
> | 상대 차례에 상대의 횟수제한 증강(`N3a` 등) 버튼이 내 화면에 떴다. 눌러도 곧 덮어써져 '발동이 안 되는' 것처럼 보였다 | 버튼을 내 차례 · 내 증강에만. 발동하면 **바로 판을 전송** (예전에는 다음 수까지 상대 화면이 옛 판) |
> | 비밀 증강 `B1b`(비숍 고정 · 인접 적 제거)가 온라인에서 한 번도 발동하지 않았다 | 상대가 둔 직후 훅을 '둔 쪽' 화면에서 돌렸는데, 그 화면에는 내 비밀 증강이 가면(`?b1`)이라 구현이 비어 있었다. **판을 받은 쪽**이 자기 증강으로 돌린다 |
> | 상대가 도감을 열어 두면 내 화면에 "상대가 증강을 고르는 중" 이 뜨고 상대 시계가 공짜로 섰다 | 도감·규칙·계정 창은 시계를 멈추지 않는다. '증강을 고르는 중' 은 드래프트가 보내는 신호(`note('draft')`)로만 뜬다 |
> | 상대가 증강을 고르는 동안 뭘 기다리는지 알 수 없었다 | 판 가운데 안내 + 오른쪽에 **상대와 같은 남은 초**. 그동안 판을 살펴보거나 수를 예약할 수 있다 |

**남는 것 (의도한 범위)**

가면은 id 를 가리지, 판에 걸린 효과까지 지우지는 않습니다. 예를 들어 `B11b`(비숍이 퀸처럼 움직임)가 걸려 있으면
상대 화면의 활성 효과에 그 효과가 보입니다. 상대 클라이언트가 체크를 제대로 계산하려면 그 정보가 필요하기 때문입니다.
**패킷을 뜯어보는 수준은 막지 않습니다** — 개발자도구로 상대 증강을 읽는 것까지가 이 구조가 막는 범위입니다.
그 이상을 막으려면 판정을 서버로 옮겨야 하고, 그건 증강을 고칠 때마다 서버도 같이 배포해야 한다는 뜻입니다.

### 계정 — 로그인하면 전적이 이어진다

기록 카드의 **로그인** 버튼에서 아이디와 비밀번호만으로 가입합니다. 이메일은 받지 않습니다.
로그인하는 순간 이 브라우저에 있던 전적이 계정 것과 합쳐지고, 그 뒤로는 판이 끝날 때마다 계정에 적힙니다.
다른 기기에서 같은 아이디로 들어오면 같은 전적이 보입니다.

```
브라우저 ──/api/auth/* · /api/records──▶ 렌더 (relay.py) ──REST──▶ 수퍼베이스
                                              └ 키와 비밀번호 검사는 여기서만
```

- 비밀번호는 서버가 **PBKDF2 해시**로만 저장하고, 브라우저는 세션 **토큰**만 갖습니다. 토큰도 표에는 해시로만 남습니다.
- 합치는 기준은 판마다 붙는 id 입니다. 두 기기의 기록을 합쳐도 같은 판이 두 번 세이지 않습니다. 최신순 100판까지 둡니다.
- '기록 지우기' 는 로그인 중이면 계정 쪽도 같이 지웁니다.
- 서버에 수퍼베이스가 연결돼 있지 않으면(로컬 실행 등) 로그인 창이 그 사실만 알리고, 전적은 브라우저에만 남습니다.
  표 만드는 SQL 은 [`docs/분석_수퍼베이스_설정.md`](docs/분석_수퍼베이스_설정.md) 의 '계정 · 전적' 절에 있습니다.

### 대국 시계

무제한 / 3분+2초 / 5분+3초 / 10분+5초 / 20분+10초 — **기본은 5분+3초**입니다. '+뒤의 초' 는 한 수를 둘 때마다 그쪽 시계에 더해지는 증분입니다
(5분+3초면 둘 때마다 3초씩 돌려받습니다). **증강 카드를 고르거나 대상을 지정하는 동안에는 시계가 멈춥니다** — 규칙을 읽느라 시간을 잃지 않도록.
도감·규칙·계정 창은 게임과 무관하므로 시계를 멈추지 않습니다.

대신 **증강을 고르는 데는 30초 제한**이 따로 있습니다. 못 고르면 고를 수 있는 것 중에서 무작위로 하나를 집습니다.
온라인에서 한쪽이 멈추면 상대가 영영 기다리게 되기 때문입니다. '판 보기' 로 덮개를 치운 동안에는 이 시간도 흐르지 않습니다.

상대가 고르는 동안 이쪽 화면에는 **판 가운데에 '상대가 증강을 고르는 중' 안내**가, 오른쪽에는 **상대와 같은 남은 초**가 뜹니다.

### 수 예약

상대 차례(AI·온라인)에 **내 기물을 끌어 놓아 두면** 내 차례가 오는 순간 그 수가 바로 나갑니다. 보라색으로 표시되고, 턴 바의 칩이나 판 우클릭·Esc 로 취소합니다.
상대가 둔 뒤 그 수가 합법이 아니면(기물이 잡혔거나 길이 막혔거나) 조용히 취소됩니다.

### 배경

판이 주인공이라 화면을 비워 뒀더니 너무 휑했습니다. 그래서 **눈에 띄지 않는 선에서만** 채웠습니다.

- 모서리에서 스며드는 **은은한 색 번짐** — accent 램프에서 10~13% 투명도로만
- 68px **격자 결** — 가장자리로 갈수록 사라지게(마스크)
- 커다란 **기물 글리프 6개** — 4~9% 투명도, 30~47초에 걸쳐 아주 느리게 떠다님

색은 전부 accent 램프에서 가져왔습니다. **status 색(빨강·초록·주황)은 배경에 쓰지 않습니다** —
이 화면에서 빨강은 처치·체크, 초록은 대상 지정이라 배경이 그 색을 띠면 경고로 오독됩니다.
`prefers-reduced-motion` 을 켠 사람에게는 떠다니지 않습니다.

메인 화면에서는 배경이 비쳐 보이도록 `#home` 을 반투명하게 두는데, 그러면 밑에 깔린 게임 화면까지
비칩니다. 그래서 메인에 있는 동안에는 게임 화면 자체를 감춥니다(`body.athome`).

### 용어 표시

지정불가 · 제거 · 교환 · 포영 · 변이 · 비밀 여섯 가지를 용어로 정의하고, 증강 문구 안의 용어와 카드 아래 `#태그`를 같은 색으로 칠했습니다.

---

## AI

> [!TIP]
> 🟩 **수정한 내용** — 원안 README 에는 없던 절입니다.

원안에는 AI 이야기가 없지만, 혼자서도 해볼 수 있어야 해서 만들었습니다. 기물 잡는 데만 몰두하지 않도록 아래를 갖췄습니다.

- **오프닝 북 46줄** — 루이 로페즈, 이탈리안, 시칠리안, 프렌치, 카로칸, 퀸즈 갬빗, 님조인디언, 킹스인디언, 그륀펠트, 잉글리시, 레티에 더해 1.g3 · 1.f4 · 1.b3 · 1.Nc3 같은 드문 첫 수도 두 갈래씩. 증강이 하나라도 등장하면 정석을 버리고 탐색으로 전환합니다.
- **평가 함수** — 기물 가치와 위치(개막/종반 보간), 킹 안전, 폰 구조(더블 · 고립 · 통과 폰), 비숍 페어, 열린 파일의 룩, 중앙 통제, 전개 지연 감점, 개막 퀸 조기 출동 감점, 캐슬링 보너스, 그리고 증강 진행도.
- **탐색** — 반복심화 + 알파베타 + 정지탐색 + MVV-LVA/킬러/히스토리 정렬 + 전치표
  + 널무브 가지치기 + 체크 연장 + 델타 가지치기 + 증분 해시.

| 난이도 | 생각 시간 | 최대 깊이 | 성격 |
|---|---|---|---|
| 초급 | 0.32초 | 4수 | 판단이 거칠고 8% 확률로 실수합니다 |
| 중급 | 1.5초 | 8수 | 정석을 따르고 실수는 거의 없습니다(0.4%) |
| 고급 | 3.6초 | 26수 | 실수하지 않고, 증강도 판 상황에 맞춰 고릅니다 |

'최대 깊이' 는 한도이고, 실제로는 시간 안에 닿는 만큼만 봅니다. 같은 국면에서 초급은 4수, 중급은 7수 안팎까지 닿습니다.

> 🟩 **수정한 내용 — 탐색이 손해 보던 자리를 메웠습니다.**
>
> 가장 컸던 건 **루트에서 알파베타를 통째로 버리고 있던 것**입니다. 예전에는 외통수를 놓치는
> 버그를 피하려고 뿌리의 모든 수를 전체 창으로 탐색했는데, 그러면 가지치기가 하나도 안 걸립니다.
> 대신 창의 기준선을 난이도 잡음(`L.noise`)만큼만 내려서 좁은 창으로 훑도록 바꿨습니다 —
> 잡음으로 뽑힐 수 있는 범위의 수는 여전히 점수가 정확하고, 그보다 나쁜 수는 어차피 안 뽑히니
> 경계값만 알면 됩니다. 난이도의 성격은 그대로 두고 깊이만 얻었습니다.
>
> 그 밖에: **널무브 가지치기**(한 수 건너뛰고도 좋으면 그 가지를 끊음), **체크 연장**(체크를 거는
> 수는 한 수 더 봄), **정지탐색 델타 가지치기**, **증분 Zobrist 해시**(매 노드 64칸을 다시 훑던 것을
> 바뀐 칸만 XOR). 전치표의 외통 점수를 ply 로 보정하지 않아 깊이마다 외통 거리가 틀어지던 것도 고쳤습니다.
>
> **측정** — 같은 시간 예산에서 도달 깊이가 초급 1→2, 중급 2→3.5, 고급 3→5.5 로 올랐습니다.
> 고친 AI 와 고치기 전 AI 를 색을 바꿔 가며 직접 붙였습니다.
>
> | 조건 | 결과 |
> |---|---|
> | 고급 설정 14판 | **10승 1패 3무 (82%)** |
> | 중급 설정(잡음 유지) 10판 | **7승 2패 1무 (75%)** |
>
> 이때는 난이도별 잡음·실수 확률을 건드리지 않았습니다(초급 14%).
>
> **이후 난이도를 한 단계씩 올렸습니다.** 초급 3수 → 4수 · 실수 14% → 8%, 중급 6수 → 8수 · 생각 시간 0.9초 → 1.5초 · 실수 2% → 0.4%,
> 고급 20수 → 26수 · 2.6초 → 3.6초. 새 중급과 예전 중급을 붙여 15판에서 10승 2패 3무였습니다.
> 초급은 여전히 일부러 틀립니다 — 다만 덜 자주.

---

## 폴더 구성

> [!TIP]
> 🟩 **수정한 내용** — 원안 README 에는 없던 절입니다.

```
├─ 무제체스 실행.bat        ← 더블클릭해서 실행
├─ README.md
├─ CLAUDE.md                 작업 규칙 (기록 · 브랜치)
├─ render.yaml               Render 배포 설정 (서비스 하나로 게임 + 중계)
├─ requirements.txt          비어 있음 — 파이썬 프로젝트로 알아보게 하려고 둔다
├─ docs/
│   ├─ 기획원문.md                 ← 원안 README 전문
│   ├─ 기획검토.md                 ← 개정 사유 35건
│   ├─ 무제체스_증강표_v2.xlsx     ← 게임에 적용된 증강 72종
│   ├─ 무제체스_증강_구현표.xlsx   ← 72종을 어떻게 구현했는지 (코드에서 자동 생성)
│   ├─ 수정한_내용.md              ← 날짜·시간별 변경 기록
│   ├─ 개발_기록.md                ← 개발 대화 전문 (자동 추출)
│   ├─ 분석_수퍼베이스_설정.md     ← 대국 통계 표 만드는 순서
│   └─ 피드백.md                   ← 받은 피드백 원문
├─ dev/active/               진행 중인 작업의 계획 · 체크리스트
├─ game/
│   ├─ index.html
│   ├─ style.css
│   ├─ wds-tokens.css        디자인 토큰 (자동 생성)
│   └─ src/
│       ├─ data.js       증강 72종의 문구·메타 (자동 생성)
│       ├─ engine.js     체스 엔진 (이동 생성, 체크, 증강용 이동 규칙 확장)
│       ├─ augments.js   증강 72종의 실제 구현
│       ├─ net.js        온라인 대전 (상태 직렬화 · 비밀 증강 가면)
│       ├─ stats.js      대국 통계 (판이 끝날 때 요약 한 번 전송)
│       ├─ account.js    계정 (로그인 · 전적 합치기)
│       ├─ game.js       턴 흐름 · 드래프트 · 시계 · 승리 판정
│       ├─ ai.js         AI (오프닝 북 · 평가 · 탐색 · 난이도 3단계)
│       ├─ sfx.js        효과음 (Web Audio 합성, 외부 파일 없음)
│       └─ ui.js         메인 화면 · 판 · 드래그 · 패널 · 모달
├─ server/
│   └─ relay.py          정적 파일 + WebSocket 중계 + 통계 · 계정 API (파이썬 표준 라이브러리만)
└─ tools/
    ├─ augment_data.py       증강 데이터 단일 소스
    ├─ build_xlsx.py         → docs/무제체스_증강표_v2.xlsx
    ├─ build_js.py           → game/src/data.js
    ├─ build_impl_xlsx.py    → docs/무제체스_증강_구현표.xlsx
    ├─ build_tokens.py       → game/wds-tokens.css
    ├─ export_devlog.py      → docs/개발_기록.md
    ├─ bump.py               index.html 캐시 스탬프 갱신
    ├─ launch.py             로컬 서버 + 브라우저 실행
    └─ test/                 브라우저 테스트 (헤드리스 크로미움 · README.md 에 돌리는 법)
        ├─ harness.js            두 화면을 한 방에 앉히는 공용 코드
        ├─ online.js             온라인 두 화면 — 예약 수 · 상대 증강 선택 · 시계 · 알림 · 발동 동기화 (26항목)
        └─ augments.js           증강 72종 — 조건이 맞는 판에서 훅이 실제로 판을 바꾸는지 (74 시나리오)
```

### 증강 문구를 고치려면

**단일 소스는 `tools/augment_data.py`** 입니다. `game/src/data.js` 나 엑셀을 직접 고치지 마세요.

```bash
cd tools
python build_xlsx.py        # -> docs/무제체스_증강표_v2.xlsx
python build_js.py          # -> game/src/data.js
python build_impl_xlsx.py   # -> docs/무제체스_증강_구현표.xlsx
python bump.py              # JS/CSS 를 고쳤으면 필수 (브라우저 캐시 무효화)
```

---

## 검증

> [!TIP]
> 🟩 **수정한 내용** — 원안 README 에는 없던 절입니다.

브라우저에서 자동으로 돌려 확인한 내용입니다.

- **증강 72종** 각각을 양측에 부여하고 한 판씩 진행 → 오류 0, 경고 0
- **자연 진행 게임 60판** (8,880수, 드래프트 461회) → 오류 0, 경고 0
- **합법수 정확성** — 속도를 위해 넣은 최적화 경로와 정공법을 1,500국면에서 대조 → 불일치 0 (체크 국면 35개 포함)
- **전술 문제**
  - 1수 외통: 고급 5/5, 중급 5/5, 초급 3/5 (초급은 의도적으로 실수함)
  - 독든 폰(퀸으로 폰을 잡으면 퀸을 잃는 자리): 세 난이도 모두 0/6 — 재료만 밝히지 않음
- **속도** — 약 83,000 국면/초 (2026-09-19 최적화 전 50,000. 미들게임 국면 · node 기준)
- **온라인 대전** — 브라우저 두 탭으로 실제 대국. 착수 전달, 증강 드래프트, 재접속(새로고침), 비밀 증강 은닉을 각각 확인했습니다.
  - 흑이 비밀 증강 `B1c` 를 얻은 뒤 백 클라이언트의 메모리 전체를 문자열로 훑어 `B1c` 가 **한 번도 나오지 않음** 을 확인
  - 그 증강이 2턴 뒤 발동하자 백 화면에 정식 id 와 문구가 공개되는 것까지 확인
  - 흑이 새로고침한 뒤에도 자기 비밀 증강을 그대로 읽고, 백은 여전히 가면만 보는 것을 확인

- **평가·탐색 최적화 회귀** (`tools/test/ai.js`) — 5항목. 2026-09-19 에 `evaluate` 를 2배 넘게 빠르게 만들면서
  자료구조를 바꿨는데, 값이 한 점이라도 달라지면 AI 가 다른 수를 둡니다. 숫자를 박는 대신 **예전 방식을 그대로
  옮겨 적은 기준 함수**와 무작위 판 400개에서 대조합니다.
- **엔진 규칙 회귀** (`tools/test/engine.js`) — 브라우저도 중계 서버도 없이 `node` 로 바로 도는 41항목.
  `P1b` 폰 전진 거리, **지정불가**(처치 대상에서 빠지는지 · 자기도 못 움직이는지 · 정해진 ply 에 풀리는지),
  **포영**(칸이 비는지 · 제자리로 돌아오는지 · 복귀 칸을 차지한 기물이 제거되는지)과 킹 보호 · 룩 면역(`R3c`).
- **증강 72종 발동 감사** (`tools/test/augments.js`) — 증강마다 조건이 성립하는 판을 만들어 얻기 · 처치 · 이동 · 턴 시작 · 체크를 일으키고
  판 · 효과 · 플래그가 실제로 바뀌는지 확인. 79 시나리오 전부 통과 — 로컬 흐름에서 발동하지 않는 증강은 없습니다.
  (`B3c` 는 스폰 칸이 막힌 종류가 고른 뒤 '불가' 가 아니라 **처음부터 잠겨서** 오는지, `K1b` 는 두 번째 선택이 **같은 칸**에서 오는지도 함께 봅니다.)
- **피드백 항목을 실제 화면에서** (`tools/test/feedback.js`) — 48항목. 판만 차려 놓고 그 뒤로는 `Game.api` 를 손대지 않고
  **사람과 같은 길로만** 간다 — 칸을 눌러서 두고, 드래프트 카드를 누르고, 대상 지정 바에서 칸을 찍는다.
  그래서 지정불가가 걸린 기물이 **잡을 칸으로 안 찍히는지**, 포영된 기물이 **판에서 사라졌다 돌아오는지**,
  막힌 선택지가 **잠긴 채로 오는지**, 부활 선택이 **언제 뜨는지** 까지 잡힙니다.
- **온라인 두 화면 테스트** (`tools/test/online.js`) — 실제 화면을 조작해 26항목: 수 예약이 내 차례에 나가는지, 상대 증강 선택 안내와 남은 초,
  드래프트 중 시계 정지, 도감을 열어도 시계가 가는지, 상대 차례에 상대 버튼이 없는지, 발동 결과가 바로 오는지, `B1b` 가 받은 쪽에서 도는지,
  상대 증강으로 사라진 칸이 빛나는지, 턴 시작 예약 증강(`B3a`)의 프롬프트와 소환된 폰이 양쪽에 오는지.

만들면서 찾아 고친 결함입니다.

1. `onExpire` 훅 시그니처 불일치
2. `R11c`(자유 캐슬링)로 킹이 룩 옆에 있을 때 캐슬링 좌표가 판을 벗어나 룩이 사라지던 문제
3. **루트 탐색이 알파베타의 '경계값'을 정확한 점수로 착각하던 문제** — 난이도 잡음이 더해지면서 외통수를 찾고도 엉뚱한 수를 두었습니다. 루트를 전체 창으로 탐색하고 잡음은 수를 고르는 단계에만 주도록 고쳤습니다.
4. **효과 지속 시간이 한 수씩 모자라던 문제** — "다음 상대턴 동안" 으로 건 지정불가 · 포영이 상대가 두기도 전에 풀렸습니다. 만료 검사가 턴 수를 올린 *뒤* 에 도는데 만료 시점을 그 앞으로 잡고 있어서, 실제로는 0턴짜리였습니다.
5. **증강을 열어 준 그 처치가 정작 그 증강의 대상에서 빠지던 문제** — `P1c`("적을 처치한 폰이 …") · `B3c`("방금 적을 처치한 비숍이 …") 는 처치로 열리는 증강인데, 처치 훅이 드래프트보다 먼저 돌아서 조건을 만든 그 수에서 발동하지 않았습니다. 획득 직후 그 처치를 한 번 더 흘려보내도록 고쳤습니다.
6. **`P1b`(두 번 전진)가 이미 움직인 폰에게도 세 칸을 주던 문제** — 처음 놓인 폰만 (2칸 + 1칸)으로 세 칸이고, 이미 움직인 폰은 (1칸 + 1칸)으로 두 칸입니다.
7. **메인 화면에서 연 증강 도감 · 규칙이 게임을 시작해야 보이던 문제** — 모달이 메인 화면보다 아래에 깔려 있었습니다.
8. **온라인에서 상대 차례에 상대의 횟수제한 증강 버튼이 내 화면에 뜨던 문제** — 누르면 내 판만 바뀌고 곧 덮어써졌습니다.
9. **비밀 증강 `B1b` 가 온라인에서 한 번도 발동하지 않던 문제** — 훅을 도는 화면에서는 가면이라 구현이 비어 있었습니다.
10. **비밀도 아닌 증강이 상대 화면에 "비밀 증강이 공개되었습니다" 로 뜨던 문제.**
11. **도감 · 규칙 창이 시계를 멈추던 문제** — 상대 차례에 열어 두면 상대 시간이 서고, 내 화면에는 '증강을 고르는 중' 이라고 떴습니다.
12. **`P11a`(폰이 두 칸까지 움직이고 처치) 앞에서 체크가 잡히지 않던 문제** — 이동 생성은 두 칸 대각 처치를 내는데 공격 판정(`attacked`)은 한 칸으로 굳어 있었습니다. 폰이 킹을 잡을 수 있는 자리인데도 '안전한 칸' 으로 봐서 체크 · 체크메이트 · 스테일메이트가 함께 틀어졌습니다.
13. **`P11a` 폰에게 앙파상이 걸리지 않던 문제** — 전진 루프와 '초기 2칸' 규칙이 같은 칸을 두 번 내는데 먼저 나온 쪽에 `double` 표시가 없어, 화면이 그걸 집는 바람에 `G.ep` 가 서지 않았습니다.
14. **`R3c` 룩의 지정불가 면역이 구현돼 있지 않던 문제** — 카드 문구의 네 가지(제거 · 포영 · 교환 · 지정불가) 중 앞의 셋만 엔진이 막고 있었습니다.

**아직 재보지 못한 것:** 난이도별 실제 대국 승률. 증강이 얹힌 한 판이 오래 걸려 시뮬레이션이 계속 타임아웃됐습니다. 위 전술 문제와 탐색 깊이 차이가 근거이고, 승률 표는 없습니다.
